<div class="container">
    <h2>To Do List</h2>
    <p>오늘 할 일 :) </p>
    <table class="table">
        <thead>
            <tr>
                <th>우선순위</th>
                <th>할 일</th>
                <th></th>
            </tr>
        </thead>
            <% @articles.each do |article| %>
    <tr>
        <td><%= article.title %></td>
        <td><%= article.content %></td>
        <td><%= link_to 'Click!', article_path(article) %></td>
    </tr>
    <% end %>
    </table>
</div>
<center><a href="/"><button type="button" class="btn btn-default">이전화면</button></a>
<a href="/articles/new"><button type="button" class="btn btn-default">리스트 작성</button></a></center>
<style>
 body {background-color: #ECCEF5; font-size:20pt; font-family: 'Do Hyeon', sans-serif;}
</style>

<link href="https://fonts.googleapis.com/css?family=Do+Hyeon" rel="stylesheet">
