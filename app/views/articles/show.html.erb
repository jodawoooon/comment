<p>
    <strong>우선순위: </strong>
    <%= @article.title %>
    </p>

<p>
    <strong>할 일: </strong>
    <%= @article.content %>
</p>

<button type="button" class="btn btn-default"><%= link_to "삭제" ,article_path(@article), method: :delete %></button>
<button type="button" class="btn btn-default"><%= link_to "수정", edit_article_path(@article) %></button>
<button type="button" class="btn btn-default"><%= link_to "목록", articles_path %></button>
<style>
 body {background-color: #ECCEF5; font-size:20pt; font-family: 'Do Hyeon', sans-serif;}
</style>
<link href="https://fonts.googleapis.com/css?family=Do+Hyeon" rel="stylesheet">

<p>
    <h3>Comments</h3>
    <ul id="comments">
        <% @article.comments.each do |comment| %>
            <li><%= comment.content %>
                <%= link_to '삭제', comment_path(comment), :method => :delete,
                data: {confirm: "정말 삭제하시겠습니까?"} %>
            </li>
        <%end%>
    </ul>
</p>

<p>
    <%= form_for :comment, url: article_comments_path(@article.id) do |f| %>
        <%= f.text_area :content %>
        <%= f.submit %>
    <%end%>
</p>